{
  "default": {
    "timeout": 120
  },
  "db": {
    "table": {
      "note": {
        "name": "${self:custom.base}-notes"
      }
    }
  },
  "s3": {
    "bucket": {
      "name": "${self:service}-${self:provider.stage}-bucket"
    }
  }
}
